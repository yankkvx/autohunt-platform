import { Box } from "@mui/material";
import Header from "../components/Header/Header";

const HomeScreen = () => {
    return (
        <Box sx={{ display: "flex" }}>
            <Header />
            HomeScreen
        </Box>
    );
};

export default HomeScreen;
